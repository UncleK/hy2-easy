# Local secrets and environment overrides
.env
.env.local
infra/hysteria.private.yaml

# Rendered deployment artifacts
rendered/

# Node-specific private source config
infra/node.yaml

# Backups and transient state
state/backups/
state/*.json
state/*.log

# Editor / OS noise
.DS_Store
Thumbs.db

# Python cache
__pycache__/
*.py[cod]

# Private test runs and local release packaging
.work/
dist/
*.pem
*.key
share.txt
share.png
client.json
