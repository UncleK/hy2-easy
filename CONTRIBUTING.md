# 参与开发

保持“安装 → 扫码或粘贴链接 → 连接”的范围。新增能力优先考虑是否确实减少用户步骤。
不要引入控制面板、账户后台、遥测端点，或将个人部署地址与凭据加入示例。

修改管理工具后，需要更新 `install.sh` 中 `TOOL_SHA256` 为 `scripts/hy2_easy.py` 的 SHA-256。
它用于校验独立安装脚本下载的固定标签文件；单元测试会验证两者一致。
所有脚本使用 UTF-8、LF 换行。

提交前运行单元测试和 ShellCheck；安装、配置、权限、分享或卸载逻辑变动还要运行隔离容器端到端测试。
如报告连接问题，请提供操作系统、客户端版本与已脱敏的错误信息，不要上传二维码或原始配置。

发布新版本时同步 Python/Bash 版本号、README 固定下载链接和核心校验和；
从已通过 CI 的提交创建标签，并附带 `install.sh` 和 `SHA256SUMS`。
